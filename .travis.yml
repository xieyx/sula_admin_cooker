
language: node_js
node_js:
  - lts/*
install:
  - yarn install # npm ci
script:
  - yarn build
deploy:
  provider: pages
  skip_cleanup: true
  local_dir: dist
  github_token: $TRAVIS_CI
  keep_history: true
  on:
    branch: master
